{
  "name": "echarts-widget",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "build": "esbuild srcjs/index.js --bundle --minify --outfile=shinyecharts/srcjs/echarts-bindings.js",
    "build-dev": "esbuild srcjs/index.js --bundle --outfile=shinyecharts/srcjs/echarts-bindings.js",
    "prettier": "prettier srcjs --write",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Stefan Kuethe",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.20.0",
    "prettier": "^3.2.5"
  },
  "dependencies": {
    "echarts": "^5.4.3"
  }
}
